# Customer Twin MVP — backend dependencies
# Install with: uv pip install -r requirements.txt   (inside .venv)
# or:           uv venv && uv pip install -r requirements.txt

fastapi>=0.111,<0.116
uvicorn[standard]>=0.29,<0.31
pydantic>=2.6,<3
pandas>=2.2,<2.3
numpy>=1.26,<2.1
scikit-learn>=1.4,<1.6
joblib>=1.3,<1.5
pytest>=8.0,<9
